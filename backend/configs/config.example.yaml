env:
  mode: "development"

database:
  path: "data/main.db"

storage:
  assets-path: "data/storage/assets"
  contents-path: "data/storage/contents"

server:
  port: ":8080"
  session-store:
    private-key: ""
