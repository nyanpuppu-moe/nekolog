햄버거  -  타이틀  ---  홈  -  로그인

대분류
타이틀
서브타이틀
해시테그

저자 - 작성일 --- 읽는데 걸리는 시간 - 편집버튼

콘텐츠

다음, 이전

어쩌고...

푸터: (운영자명 © 2026 김운영 (Operator)) --- (운영자추가가능링크: 소개 방명록 피드 등)
----라인
Made with 💖 by FlatBlog Engine • 자유로운 독립형 마이크로 블로그 플랫폼

# 에디터 및 플러그인 구현 (milkdown)

- [ ] BlockEdit 핸들 숨기기: shouldShow 작동 불가로 css 처리하기 (.milkdown-block-handle {display: none;})
- [ ] 콜라보레이션 플러그인: nodejs 별도 서버로 구현하기
- [ ] 히스토리 플러그인 (@milkdown/plugin-history, @milkdown/kit/plugin/history): 탑바에 버튼 아이콘 추가하기
- [ ] 협업 편집 라이브 서버: 추후 liveserver 패키지 만들어서 가이드대로 구현하기
- [ ] 린트 시스템 기능: h1, h2 순서 안 지키거나 alt 누락 시 VSC처럼 경고 띄워주는 milkdown 플러그인 조사 및 적용하기
- [ ] 이미지 마크업 대응: 게시글 안의 날짜 등이 <time> 안에 묶일 수 있는지 milkdown 및 remark에서 방법 찾아보기
- [ ] 블록인용문(>) 스타일 점검: 인용문 영역에 정보, 경고, 주의 등의 분기 스타일 디자인이 마련되어 있는지 확인하고 적용하기
- [ ] 하이라이터: 하이라이팅 기능이 구현 가능한지 확인해보기 (<mark>)
- [ ] 첨자: <sub> <sup> 구현 가능한지 확인

# 뷰어 및 블로그 기능 (View Mode)

- [ ] 목차(TOC) 기능: 뷰 모드에서 목차 보여주기 구현하기
- [ ] Frontmatter 연동: 맨 앞에 --- 넣어서 게시글 정보 처리하는 기능 고려하기
- [ ] 게시글 폴더화(그룹): 그룹 내 게시글 주르륵 뜨는 기능 구현 (작성자가 켜고 끌 수 있게 개발). 특히 그룹은 / 를 통해 path 로 여러 깊이로 구성 가능하게
- [ ] 연관 게시글 추천: search keyword 기반 추천 또는 명시적 해시태그 생성 기능을 유저가 선택하도록 구현하기
- [ ] 다음/이전 글 탐색: 유저에게 필드 입력 추가하고, 특히 이전 글은 DB에서 찾아서 자동 추천해주기
- [ ] 리딩 프로그레스 바: 본문 스크롤에 따른 진행도 표시바 고려하기
- [ ] 읽기 시간 계산: 평균 읽기 시간을 적당히 계산해서 타이틀 아래에 보여주기 기능 만들기
- [ ] URL 앵커 동기화: 로드 후 시간 지나면 스크롤 위치에 맞춰 현재 보이는 요소의 `h id`를 URL `#` 뒤에 `replaceState로` 붙여주기
- [ ] RSS 피드: 블로그 구독을 위한 RSS 피드 생성 기능 고려하기
- [ ] 코드 복사 버튼: 클라이언트 사이드에서 느리게 굽도록 추가 (서버 사이드는 관여 안 함)
- [ ] 브레드크럼 및 폴더 뷰: Breadcrumb 기능을 넣고 zed 에디터처럼 패스에 따라 폴더 구조로 시각화하기. 아마 타이틀 바로 아래에 넣으면 될듯
- [ ] rehype-slug rehype-autolink-headings 를 넣어서 글 제목에 대해 앵커링크 다는 플러그인 추가.
- [ ] rehype-external-links 달아서 외부 링크를 aria-label 주입 될 수 있게 하기

# 미디어 및 성능 최적화

- [ ] 이미지 여러 개 보여주기: 이전에 만들어둔 banner 구현 코드 뜯어와서 적용할지 고민 및 해결하기
- [ ] 클라이언트 사이드 WebP 변환: 모든 이미지는 업로드 전 웹 브라우저 Canvas API로 WebP 재인코딩해서 용량 줄이고 업로드 속도 올리기
- [ ] 이미지 크기 속성 자동 주입: WebP 변환 시 width, height를 뽑아 파일명에 남기고, 빌더 단에서 추출해 img width height 주입하기 (로드 전 레이아웃 유지용)
- [ ] ThumbHash 도입 및 SSR 연동: 블러해시 대신 ThumbHash를 써서 SSR 단에서 이미지나 그레디언트로 구워내기. 해시 뽑는건 canvas api 상에서 클라이언트가 미리 처리하기
- [ ] 관리자 기능 추가: 관리자가 시스템 폰트를 변경할 수 있는 설정 방법 추가하기
- [ ] 스크립트 온디맨드 호출: 스크립트 크기가 커지지 않게 필요할 때 지연 로딩 처리하기. 예를 들어 mermaid 나 vim이동은 느리게 로드 해도 됨
- [ ] UML 렌더러 지연 임포트: 페인트 완료 일정 시간 후 레이지 로딩하고, 기본 크기 고정 및 클릭 시 전체 화면 확대 구성하기 (cursor: zoom-in; 및 aria-label="다이어그램 확대 보기" 필수)
- [ ] 유튜브나 트위터, 영상들 처리할 방법 생각하기

# 키보드 및 내비게이션 접근성

- [ ] 툴바엔 aria-label와 role="toolbar" 가 잘 들어있는지 확인해야함
- [ ] 건너뛰기 링크 (Skip Navigation): 탭 키 친화 구현으로 누르면 본문으로 바로 이동하는 링크 뜨게 만들기
- [ ] 포커스 링 스타일 고정: focus-visible 속성에 대해 포커스 링 디자인을 고정하고, 최초 등장 시 위치 파악이 잘 되도록 약간의 애니메이션 추가하기. prefer 따라 애니메이션을 안 할수도 있음
- [ ] 코드 스니펫 키보드 탐색: <pre tabindex="0" aria-label="코드 스니펫 스크롤 영역"></pre> 적용하여 탭 진입 후 방향키(←, →)로 코드 넘겨볼 수 있게 하기
- [ ] 앵커 링크 포커스 이동: <h2 id="id" tabindex="-1"> 형태로 구현해서 자바스크립트나 앵커 클릭 이동 시 포커스도 함께 넘어가도록 처리하기.
- [ ] 키보드 핫키 네비게이션: 탭 키 사용 시 노란색 박스에 단축키를 적어줘서 해당 키 입력 시 바로 포커스 이동하게 만들기 (사이드바 이동 편의성 향상용, aria-hidden="true" 필수). 약간 빔 f 처럼 작동
- [ ] 탑 이동 버튼 우회 안내: 위로 이동하기 버튼 주변에 마우스용 외에 탭 포커스 시 보일 kbd 테그 감싼 Home 안내 span 추가하기 (aria-label="Home 키를 눌러 최 상단으로 이동" 달기)
- [ ] 스크롤 여백 확보: scroll-margin-top 속성을 설정해서 탭 키나 앵커로 이동 시 화면 내부 요소가 상단 고정 탑바에 가려지지 않게 처리하기

# 스크린 리더 친화 구현 (정적 마크업)

- [ ] 새 창 링크 안내: 새 창으로 열리는 링크 뒤에 <span class="sr-only">새 창으로 열림</span>이 읽히도록 구현하기
- [ ] 시맨틱 태그 활용: main, header, footer, article, nav 태그를 명확히 구분해서 마크업하기
- [ ] 탐색 영역 레이블링: main 영역 안의 연관 글 및 이전/다음 글 이동은 <nav aria-label="게시글 탐색"> 구조로 감싸기
- [ ] 현재 페이지 표시: 현재 페이지 링크에는 aria-current="page" 속성 부여하기 (CSS a[aria-current='page'] 선택자로도 활용)
- [ ] 불필요 요소 숨김: 스크린 리더 기계가 읽을 필요 없는 요소는 aria-hidden="true"로 가려주기
- [ ] 입력 창 설명 연결: 모든 input 요소에는 반드시 설명 텍스트의 ID를 aria-describedby로 연결하기
- [ ] 버튼 표준 태그 준수: 클릭되는 모든 요소는 무조건 div 대신 표준 <button> 태그로 구현하기
- [ ] 리스트 불릿 요소 대응: li 태그에 들어가는 bfr(before) 스타일 요소들에 적절한 aria 속성 달아주기 (잘 읽을 수 있게)
- [ ] rehype-accessible-emojis 달아서 이모지를 스크린리더가 읽을 수 있게 하기

# 동적 상태 및 대화상자 접근성

- [ ] 동적 상태 알림창: 바텀 상태 표시 등 댓글 성공 메시지가 뜨는 곳에 <div role="status" aria-live="polite">를 걸어 스크린 리더가 하던 말을 끝낸 후 읽어주도록 구현하기
- [ ] 가상 로딩 바 스크린 리더 대응: 깃허브 스타일의 페이크 로딩바 구현 시 aria-busy="true" 및 aria-label="글 불러오는중" 속성 넣어서 로딩 상태 알리기
- [ ] 사이드바 웹 표준 준수: qwreeyjs의 fulloverlay (웹 표준 <dialog>) 사용하기. 사이드바 메뉴 여는 버튼에 aria-haspopup="dialog" 속성 부여하기

# 프린트 및 접근성, 테마 스타일링

- [ ] 프린트 시 스타일 고정: 프린트 상태 시 라이트 테마로 고정하고 불필요한 포맷 제거하기
- [ ] 프린트 레이아웃 최적화: 프린팅 상태일 때 푸터, 헤더 및 기타 주변 요소를 display: none;으로 제거하고 본문 스타일 다듬기
- [ ] 프린트 URL 주소 자동 표시: @media print 내에 article a[href^="http"]:after { content: " (" attr(href) ") "; font-size: 85%; color: #333; } 스타일 추가하기
- [ ] 프린트 글자 잘림 방지: 요소들에 page-break-inside: avoid; 속성 넣어서 인쇄 시 글자 잘림 현상 절대 방지하기
- [ ] 고대비 테마 대응: @media (forced-colors: active) 및 @media (prefers-contrast: more) 미디어 쿼리를 사용해 고대비 모드 완벽 대응하기
- [ ] 모션 감축 대응: 애니메이션 효과에 prefers-reduced-motion을 따르도록 구현하고, 뷰 트랜지션(View Transitions) 사용 시에도 감축 모드면 모션을 즉시 지우도록 처리하기
- [ ] 테마 상태 깜빡임 방지 (FOUC): 다크/라이트 테마 선택 여부를 쿠키에 저장하고, 서버 사이드에서 이 쿠키를 읽어 head 안에 style 인라인 주입으로 배경색을 미리 두어 화면 깜빡임 방지하기 (클라에선 document.cookie 변경 대응, 테마 설정이 없을 땐 시스템 미디어 쿼리 우선 작동)
- [ ] 팝오버 구현 시 popover 를 사용하고 (Popover API) haspopup="menu" aria-expanded="true" 꼭 지키기. 위치 놓는건 anchor positioning 사용

# 다국어 지원

- [ ] 다국어 레이블 지원: 브라우저의 Accept-Language 헤더 값에 맞춰 기본 레이블 언어가 동적으로 달라지도록 구현하기
- [ ] 다국어 게시글 lang 속성: html lang="en" 등의 문서 기본 언어 속성을 게시글 작성자가 설정한 언어에 맞춰 동적으로 변경할 수 있도록 구현하기
- [ ] @lingui/core 기반 라벨 굽기

# 검색엔진 최적화

- [ ] OG 뽑아서 넣는것 (글 작성자가 설정 가능하게)

# 최종

- [ ] CNAME 가능하도록 구성

# 기타 앨랠래

프린트 시 스타일을 라이트로 고정하고 포멧 빼고 어쩌고 하기

font-display: swap; 넣어서 깜빡이지 않게 하고
관리자가 폰트를 변경 할 수 있는 방법을 추가하자..
다음 글과 이전 글을 선택 할 수 있도록 유저에게 필드 입력 추가. 특히 이전 글은 db 에서 찾아서 추천해주기
게시 글들은 폴더화 되어있으면 좋긴 하겠음. 그룹으로써 그 안에 있으면 나중에 이 그룹 게시글 보기를 작성자가 켜놓으면 주르륵 뜨는식.
아니면 연관 게시글 보여주기를 search keyword 가지고 두게 한다던가 하는식을 유저가 선택해서 놓을 수 있으면 좋겠음. 또는 명시적 해시 태그 만들기
h1 h2 순서를 안 지킨다던가 alt 안 달아버린다던가 하는건 vsc 처럼 경고 띄워주는 lint 가 있으려나 milkdown 플러그인 찾아봐야할듯
이미지 여러개는 어떻게 보여주지? 이전에 만들어둔 banner 구현 뜯어와야할까 고민중
모든 이미지는 웹 브라우저에서 webp 굽기 해야할듯

탭키 친화 구현
탭키로 본문으로 이동 뜨도록 (구글 검색이나 깃허브에서 탭 키 누르면 바로 뜨는 그것)
html lang=en 이 블로그 글 게시자에 의해 변경할 수 있도록 구현
focus-visible 속성에 대해 링 디자인 고정 구현되도록. 포커스 링은 초기 등장에만 약간의 애니메이션을 넣어 어디 있는지 잘 보이게 구현

스크린리더 친화 구현
새 창에 열리는 링크는 sr-only span 으로 새 창으로 열림 이 스크린리더에서 읽히도록 구현
발행일 같은것 들은 <time> 안에 묶이게. 근데 이거 게시 글 안에 날짜도 묶어질까? milkdown 과 remark 에서 방법 찾아보기
눌리는 버튼은 무조건 button 으로 구현

다른 접근성
prefers reduced motion 을 따르도록 수현
main, header, footer, article, nav 등을 사용해서 구현
main 안에 연관 글 이동, 이전 다음 글 이동은 nav 안에 감싸는데 aria-label="게시글 탐색" 달아서 스크린 리더 읽기 편하게 만들기
이 페이지에 대한 링크는 aria-current="page" 를 달아주기. css 선택 시에도 a[aria-current='page'] 가 가능해서 편할것임
aria-hidden="true" 로 기계에 필요 없는것은 가려주기
div role="status" aria-live="polite" 를 걸어둔 곳에 내용을 동적으로 넣으면 (예: 댓글이 성공적으로 등록되었습니다. 바텀 상태표시 등) 스크린 리더가 말을 다 끝낸 다음 읽어주게됨
인풋에는 무조건 aria-describledby 를 넣어 설명 텍스트를 id 로 연결해줘야함
위로 이동하기 버튼을 아래 두고 마우스로 클릭 할 수도 있지만, 만약 탭으로 뭔가 포커스 되어 있다면 Home 버튼을 누르게 유도하게 약간 키보드 모양으로 감싼 span 에 Home 이라 적어 보여주자. 키보드 모양은 kbd 테그 사용 aria-label 은 'Home 키를 눌러 최 상단으로 이동' 으로
사이드바는 qwreeyjs 의 fulloverlay 사용(웹 표준 dialog) aria-haspopup='dialog' 를 메뉴를 여는 버튼에 넣어 누르면 메뉴가 열림을 알 수 있도록 두기
뷰 트렌지션을 사용. prefer reduced motion 이면 모션을 지워버리기
로드 중에는 깃허브와 같이 페이크 로딩바를 넣어야함. 이건 aria-busy="true" aria-label="글 불러오는중" 넣어서 스크린 리더도 알 수 있도록

프린팅 상태면 스타일 빼기(푸터 해더 다른 요소 모두 display none 으로 날리고, 스타일 다듬기)
**@media (forced-colors: active)**랑 **@media (prefers-contrast: more)**
따라 고대비 테마 대응하기

**Accept-Language** 따라 label 언어 달라지도록


@media print {
/* 본문 내부의 외부 링크 옆에 (https://...) 주소를 자동으로 띄워주기 */
article a[href^="http"]:after {
content: " (" attr(href) ") ";
font-size: 85%;
color: #333;
}
}
달아서 프린트 시 url 전체 보이도록
page-break-inside: avoid;
넣어서 프린트시 글자 짤림 절대 방지

> 에 정보, 경고나 주의 같은거 없나 확인좀

Breadcrumb 넣고 그거 페스 따라서 마치 zed 처럼 폴더 구조로 보여주자
scroll-margin-top: 넣어서 탭 시 화면에 보이게 될 요소가 탑바 같은것에 가리지 않도록 만들기

리딩 프로그래스바에 대해 생각해보기

읽는데 걸릴 평균 시간을 적당히 계산해서 위에 타이틀 아래 써주는걸 만들까
로드 후 시간 지나면 스크롤 시 지금 보이는 곳의 h id 를 url 에 # 뒤에 붙이도록 replaceState 를 수행해주기

<pre tabindex="0" aria-label="코드 스니펫 스크롤 영역"></pre>
이럼 키보드 탭을 치다가 코드 블록에 딱 걸렸을 때 키보드 방향키(←, →)로 코드를 넘겨볼 수 있게 됨

<h2 id="rust-thread" tabindex="-1">Rust 멀티스레딩이란?</h2>
를 걸면 자스나 앵커 링크로 이동 시 포커스도 이동됨
blur hash 넣기

키보드 탭을 쓰면 노란색 박스에 키를 적어서 그 키 누르면 거기로 포커스 이동 가능하게
특히 사이드바 같은거에서 이동 편하게 넣어주기. 무조건 aria-hidden="true" 붙어야함
가능 한 경우 코드를 온 디멘드 호출해서 스크립트 크지 않게 하기. 필요 할 때 요구되는 스크립트는 지연되어도 됨
uml 렌더러는 페인트 이후 일정 시간 이후 임포트. 크기는 고정으로 두고 너무 크면 누르면 전체 화면에 확대 되도록 구성 cursor: zoom-in; 꼭 해주기aria-label="다이어그램 확대 보기" 도 해줘야함
이미지는 webp 자동 변환이 되긴 할껀데, width height 는 뽑아서 이미지 이름에 남겨버리고 나중 빌더 상 크기 뽑아서 width height 기본 주입해주는걸 넣어주기 로드 전에도 크기가 유지되도록
Canvas api 가지고 클라이언트 사이드에서 업로드 시 webp 로 재 인코딩이 가능하다는데 이걸로 업로드 전에 용량 줄여서 업로드 속도를 올리기


​1920x1080 같은 큰 이미지의 픽셀을 그대로 블러해시 알고리즘 알고리즘(DCT 연산)에 집어넣으면 브라우저가 비명을 지르면서 렉이 걸려. 그래서 압축 프로세스 중간에 딱 요 단계를 얹어주는 거야!
• ​32x32 픽셀로 강제 축소하기 WebP용 큰 캔버스 말고, 메모리에만 잠깐 쓸 32x32 크기의 아주 조그만 임시 캔버스를 하나 더 만들어 예지야~ 거기에 원본 이미지를 슥 그려서 엄청나게 뭉개진 초소형 썸네일을 만드는 거지!
• ​픽셀 데이터 뽑기 그 32x32 캔버스에서 ctx.getImageData(0, 0, 32, 32)를 호출하면, 우리가 연산해야 할 전체 픽셀 수가 고작 1,024개로 확 줄어들어!
• ​블러해시 인코딩 돌리기 공식 blurhash 라이브러리의 encode 함수는 크기가 몇 KB밖에 안 되는 아주 가벼운 순수 함수거든? 1,024개짜리 픽셀 배열을 이 함수에 던지면 루프를 엄청 적게 돌기 때문에 12밀리초(ms) 만에 연산이 즉시 끝나버려 사용자는 인코딩이 도는 줄 눈치도 못 채지

ThumbHash 를 써야할듯.  SSR 단에서 이미지나 그레디언트로 굽기

RSS 피드 기능 고려하기
코드 복사 버튼 추가하기. 복사 버튼은 느리게 클라이언트에서 굽고, 서버에선 관여 안하게
다크나 라이트 테마 유무는 쿠키에 담게, 서버 사이드에서 쿠키 따라 가본 배경 색을 다크 또는 라이트로 둬서 깜빡이지 않게 (head 안에 style 인라인 주입). 클라에선 document.cookie 바꾸면 됨
주의점은 테마 없으면 미디어 쿼리 되어야함
li 에 들어간 비포들 aria 달려야함